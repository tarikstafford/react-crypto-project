import React from 'react'
import {
    SideBarContainer,
    Icon,
    CloseIcon
} from './SideBarElements'

const SideBar = () => {
    return (
        <div>
            <h1> baloney </h1>
        </div>
    );
};

export default SideBar
